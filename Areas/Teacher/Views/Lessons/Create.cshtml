@model SchoolManagementSystem.Web.ViewModels.Teacher.LessonFormViewModel

@{
    ViewData["Title"] = Localizer["Add Lesson"];
}

<div class="form-page-header">

    <a asp-action="Index"
       class="back-link">
        ← @Localizer["My Lessons"]
    </a>

    <h1 class="page-title mt-2">
        @Localizer["Add Lesson"]
    </h1>

    <p class="page-subtitle">
        @Localizer["Schedule a lesson for one of your groups and subjects"]
    </p>

</div>

<form asp-action="Create"
      method="post">

    @Html.AntiForgeryToken()

    <partial name="_Form"
             model="Model" />

    <div class="form-actions">

        <a asp-action="Index"
           class="btn btn-light">
            @Localizer["Cancel"]
        </a>

        <button type="submit"
                class="btn btn-primary">
            @Localizer["Create Lesson"]
        </button>

    </div>

</form>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
