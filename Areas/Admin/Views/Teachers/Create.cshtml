@model SchoolManagementSystem.Web.ViewModels.Admin.TeacherFormViewModel

@{
    ViewData["Title"] = Localizer["Add Teacher"];
}

<div class="form-page-header">

    <a asp-action="Index"
       class="back-link">
        ← @Localizer["Teachers"]
    </a>

    <h1 class="page-title mt-2">
        @Localizer["Add Teacher"]
    </h1>

    <p class="page-subtitle">
        @Localizer["Create a teacher account and assign groups and subjects"]
    </p>

</div>


<form asp-action="Create"
      method="post">

    @Html.AntiForgeryToken()

    <partial name="_Form"
             model="Model" />


    <div class="form-actions">

        <a asp-action="Index"
           class="btn btn-light">
            @Localizer["Cancel"]
        </a>

        <button type="submit"
                class="btn btn-primary">
            @Localizer["Create Teacher"]
        </button>

    </div>

</form>


@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
