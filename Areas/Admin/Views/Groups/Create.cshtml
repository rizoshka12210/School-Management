@model SchoolManagementSystem.Web.ViewModels.Admin.GroupFormViewModel

@{
    ViewData["Title"] = Localizer["Add Group"];
}

<div class="form-page-header">

    <a asp-action="Index"
       class="back-link">
        ← @Localizer["Groups"]
    </a>

    <h1 class="page-title mt-2">
        @Localizer["Add Group"]
    </h1>

    <p class="page-subtitle">
        @Localizer["Create a new school group and assign teachers"]
    </p>

</div>

<form asp-action="Create"
      method="post">

    @Html.AntiForgeryToken()

    <partial name="_Form"
             model="Model" />

    <div class="form-actions">

        <a asp-action="Index"
           class="btn btn-light">
            @Localizer["Cancel"]
        </a>

        <button type="submit"
                class="btn btn-primary">
            @Localizer["Create Group"]
        </button>

    </div>

</form>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
