@model SchoolManagementSystem.Web.Models.Entities.Group

@{
    ViewData["Title"] = Localizer["Group Details"];
}

<div class="container mt-4">

    <div class="row justify-content-center">

        <div class="col-lg-7">

            <div class="card border-0 shadow-sm">

                <div class="card-body p-4">

                    <h2 class="fw-bold">
                        @Model.Name
                    </h2>

                    <hr />

                    <h5>@Localizer["Students"]</h5>

                    @if (Model.Students.Any())
                    {
                        <ul>
                            @foreach (var student in Model.Students)
                            {
                                <li>
                                    @student.FirstName @student.LastName
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p class="text-muted">
                            @Localizer["No students in this group."]
                        </p>
                    }

                    <h5 class="mt-4">
                        @Localizer["Teachers"]
                    </h5>

                    @if (Model.Teachers.Any())
                    {
                        <ul>
                            @foreach (var teacher in Model.Teachers)
                            {
                                <li>
                                    @teacher.ApplicationUser.FullName
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p class="text-muted">
                            @Localizer["No teachers assigned."]
                        </p>
                    }

                    <a asp-action="Edit"
                       asp-route-id="@Model.Id"
                       class="btn btn-primary">
                        @Localizer["Edit"]
                    </a>

                    <a asp-action="Index"
                       class="btn btn-outline-secondary">
                        @Localizer["Back"]
                    </a>

                </div>

            </div>

        </div>

    </div>

</div>
