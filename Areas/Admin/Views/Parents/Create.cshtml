@model SchoolManagementSystem.Web.ViewModels.Admin.ParentFormViewModel

@{
    ViewData["Title"] = Localizer["Add Parent"];
}

<div class="form-page-header">

    <a asp-action="Index"
       class="back-link">
        ← @Localizer["Parents"]
    </a>

    <h1 class="page-title mt-2">
        @Localizer["Add Parent"]
    </h1>

    <p class="page-subtitle">
        @Localizer["Create a parent account and connect students"]
    </p>

</div>

<form asp-action="Create"
      method="post">

    @Html.AntiForgeryToken()

    <partial name="_Form"
             model="Model" />

    <div class="form-actions">

        <a asp-action="Index"
           class="btn btn-light">
            @Localizer["Cancel"]
        </a>

        <button type="submit"
                class="btn btn-primary">
            @Localizer["Create Parent"]
        </button>

    </div>

</form>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
