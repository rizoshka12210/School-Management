namespace SchoolManagementSystem.Web;

/// <summary>
/// Marker type used only to anchor IStringLocalizer&lt;SharedResource&gt;
/// to Resources/SharedResource.resx (English) and
/// Resources/SharedResource.ru.resx (Russian). One shared resource file
/// is used for every view/controller instead of per-view resx files,
/// since most of the app's ~80 views reuse the same handful of nouns
/// and verbs.
/// </summary>
public class SharedResource
{
}
